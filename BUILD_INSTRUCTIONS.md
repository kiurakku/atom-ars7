# 🔧 Інструкції зі збірки Atom для 2026

## 📋 Вимоги

### Системні вимоги:
- **Node.js**: 16.x або 18.x (рекомендовано 18.x для Electron 16+)
- **npm**: 8.x або вище
- **Python**: 3.x (для native modules)
- **Build tools**: 
  - Windows: Visual Studio Build Tools або Visual Studio
  - Linux: build-essential, libsecret-dev
  - macOS: Xcode Command Line Tools

### Перевірка версій:
```bash
node -v  # Має бути v16.x або v18.x
npm -v   # Має бути 8.x+
python --version  # Має бути 3.x
```

## 🚀 Кроки збірки

### 1. Клонування та підготовка

```bash
# Якщо ще не клоновано
git clone <your-fork-url>
cd atom-ars7

# Або якщо вже є локальна копія
cd atom-ars7
```

### 2. Встановлення залежностей

```bash
# Встановити залежності скриптів збірки
cd script
npm install
cd ..

# Встановити основні залежності
npm install
```

**⚠️ Примітка**: Перша установка може зайняти багато часу (10-30 хвилин), оскільки потрібно завантажити Electron та скомпілювати native modules.

### 3. Збірка проєкту

```bash
# Повна збірка (рекомендовано для першого разу)
npm run build

# Або поетапно:
# 1. Bootstrap (встановлення залежностей)
node script/bootstrap

# 2. Збірка
node script/build
```

### 4. Запуск Atom

```bash
# Після успішної збірки
./atom.sh  # Linux/macOS
# або
atom  # Якщо встановлено глобально

# Windows
atom.cmd
```

## 🐛 Вирішення проблем

### Помилка: "Cannot find module '@electron/remote'"

**Рішення**:
```bash
npm install @electron/remote@^2.0.0
```

### Помилка: "node-gyp rebuild failed"

**Рішення**:
```bash
# Windows
npm install --global windows-build-tools

# Linux
sudo apt-get install build-essential python3

# macOS
xcode-select --install
```

### Помилка: "Electron failed to install"

**Рішення**:
```bash
# Очистити кеш та перевстановити
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### Помилка з native modules після оновлення Electron

**Рішення**:
```bash
# Перебудувати native modules
npm rebuild
# або для конкретного модуля
npm rebuild @atom/fuzzy-native
```

## 🧪 Тестування

### Запуск тестів:
```bash
npm test
```

### Запуск специфічних тестів:
```bash
npm test -- --grep "назва тесту"
```

## 📦 Створення дистрибутиву

### Windows:
```bash
node script/build --create-windows-installer
```

### Linux (Debian):
```bash
node script/build --create-debian-package
```

### Linux (RPM):
```bash
node script/build --create-rpm-package
```

### macOS:
```bash
node script/build --code-sign
```

## 🔍 Діагностика

### Перевірка версії Electron:
```bash
node -e "console.log(require('./package.json').electronVersion)"
```

### Перевірка встановлених залежностей:
```bash
npm list electron
npm list @electron/remote
```

### Логи збірки:
Якщо збірка не вдається, перевірте:
- `out/` - директорія з результатами збірки
- Консольний вивід на помилки
- `node_modules/.cache/` - кеш збірки

## 🐳 Docker збірка

Якщо є проблеми з середовищем, використовуйте Docker:

```bash
# Побудувати образ
docker build -t atom-build .

# Запустити збірку в контейнері
docker run -it -v $(pwd):/workspace atom-build bash
cd /workspace
npm install
npm run build
```

## 📝 Нотатки

- Перша збірка займає значно більше часу (може бути 30+ хвилин)
- Наступні збірки швидші завдяки кешуванню
- Для розробки використовуйте `npm run build -- --existing-binaries` для швидшої збірки

---

**Останнє оновлення**: 2026-01-XX
**Версія Electron**: 16.2.0
